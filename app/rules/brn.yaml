# BRN detector configuration (Korean business registration number).

enabled: true

patterns:
  - name: brn_hyphen
    regex: '(?<!\d)\d{3}-\d{2}-\d{5}(?!\d)'
  - name: brn_compact
    regex: '(?<!\d)\d{10}(?!\d)'

max_match_len: 16

checksum:
  enabled: true
